#ifndef ENUMS_H
#define ENUMS_H

enum field_color
{
    EMPTY,
    BLACK,
    WHITE
};

enum write_perms
{
    READ,
    WRITE
};

enum opposite_player
{
    HUMAN,
    COMPUTEREASY,
    COMPUTERHARD
};

#endif // ENUMS_H
